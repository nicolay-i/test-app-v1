# Эффективность

OpenCode events не сообщили provider billing usage ни для одной trajectory. Все
lifecycle usage имеют статус `unavailable`; токены, стоимость и efficiency ranking
не вычисляются и не заменяются нулями.
